package com.deadlinenote;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class DeadlineNoteApplication {
    public static void main(String[] args) { SpringApplication.run(DeadlineNoteApplication.class, args); }
}
